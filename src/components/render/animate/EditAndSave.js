/**
*   基于控制点编辑
*   1.对node节点图形编辑
*   2.对图形组合进行编辑，采用控制点方式重新生成编辑器
*   @class EditAndSave
*/
define(['Animation', 'Node', 'Factory', 'Utils'], function (Animation, Node, Factory, Utils) {
    
    var animation = Animation.self,
        factory = Factory.self,
        listenClickTap = Utils.self.listenClickTap,
        node = Node.self;

    var _es = function () {

    }


    return {
        self:_es,
    }
});